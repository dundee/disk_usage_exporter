package build

var (
	// BuildVersion is the tagged version of the binary
	BuildVersion = "<<< filled in by build >>>"
	// BuildDate is date and time of the binary build
	BuildDate = "<<< filled in by build >>>"
	// BuildCommitSha is git sha hash of the commit from which is the binary built
	BuildCommitSha = "<<< filled in by build >>>"
)
