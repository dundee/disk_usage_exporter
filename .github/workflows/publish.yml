name: Publish

on:
  push:
    branches: ['master']

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
        with:
          go-version: 1.26.x
      - uses: actions/checkout@v6

      - uses: ko-build/setup-ko@v0.9
      - run: ko build